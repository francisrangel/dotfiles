#!/usr/bin/env bash

_complete_projects() {
	local cur
	cur=${COMP_WORDS[COMP_CWORD]}
	subs=`expr "${PROJECTS_DIR}" : ".*\/\(.*\)" `
	COMPREPLY=($(compgen -S/ -d ${PROJECTS_DIR}/$cur | sed s/.*${subs}.// ))
}

complete -o nospace -F _complete_projects p
complete -o nospace -F _complete_projects pw
