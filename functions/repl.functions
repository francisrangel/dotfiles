#!/usr/bin/env bash

repl() {
	screen -S repl $1

	echo -n 'Exit terminal? Type "r" to launch the repl again... (Y/r/n): ' && read -n 1 should_exit
	[ $should_exit == 'r' ] && echo && repl $1
	[ $should_exit == 'n' ] && echo && return
	exit 0
}
